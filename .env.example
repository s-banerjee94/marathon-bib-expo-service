# Database Configuration (Docker MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=marathon_bib_expo
DB_USERNAME=root
DB_PASSWORD=root

# MySQL Root Password (for Docker)
MYSQL_ROOT_PASSWORD=root

# Application Configuration
SERVER_PORT=8080

# JPA Configuration
DDL_AUTO=update
SHOW_SQL=true
