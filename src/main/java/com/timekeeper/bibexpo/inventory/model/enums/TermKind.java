package com.timekeeper.bibexpo.inventory.model.enums;

/**
 * Which vocabulary a term row belongs to — decides the dropdown it fills.
 */
public enum TermKind {
    ITEM_CATEGORY,
    LOCATION_TYPE,
    UNIT
}
