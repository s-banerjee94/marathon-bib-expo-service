package com.timekeeper.bibexpo.messaging.provider.model.enums;

/**
 * Which campaign provider row the resolver selected for an organization on a channel:
 * the organization's own override, or the shared platform default.
 */
public enum ProviderSource {
    ORGANIZATION,
    DEFAULT
}
